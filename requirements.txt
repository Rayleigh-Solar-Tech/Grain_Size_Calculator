# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
matplotlib>=3.5.0
pandas>=1.3.0

# Machine Learning / AI (CPU optimized for high performance)
torch>=1.12.0
torchvision>=0.13.0
ultralytics>=8.0.0

# Note: Available SAM models in ultralytics 8.x:
# - sam_h.pt (huge)    - sam_l.pt (large) ✅ BEST ACCURACY
# - sam_b.pt (base)    - mobile_sam.pt    - sam2_t.pt (tiny)
# - sam2_s.pt (small)  - sam2_b.pt (base) - sam2_l.pt (large)
# - sam2.1_* (latest versions)
# Using: sam_l.pt for maximum accuracy on strong CPUs

# OCR libraries (choose based on preference)
pytesseract>=0.3.9
easyocr>=1.6.0
Pillow>=8.3.0

# GUI Framework
PyQt5>=5.15.0
# Alternative: PyQt6>=6.2.0 (uncomment if you prefer PyQt6)

# Additional image processing
scikit-image>=0.19.0
imageio>=2.19.0

# Utilities
tqdm>=4.64.0
pyyaml>=6.0

# Development and testing (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0

# For building executable (required for distribution)
pyinstaller>=5.13.0
# Additional build dependencies
setuptools>=65.0.0
wheel>=0.38.0
# Windows-specific dependencies for standalone exe
pywin32>=305; sys_platform == "win32"

# Note: For tesseract OCR, you also need to install Tesseract-OCR separately:
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
# Linux: sudo apt-get install tesseract-ocr
# macOS: brew install tesseract